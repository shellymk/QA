# Credenciais — NUNCA versionar (qualquer .env, .env.local, .env_outro, etc.)
**/.env*
!**/.env.example
bot-auth.json
debug_bot_*.png
.env
backend/.env

# Gravações das reuniões (Etapa 5) — pesadas, não versionar
backend/recordings/

# Dependências
node_modules/

# Logs
*.log
npm-debug.log*
# Screenshots de referência (não versionar)
Captura de tela*.png
Screenshot*.png
