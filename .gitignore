# Credenciais — NUNCA versionar
**/.env
bot-auth.json
debug_bot_*.png
.env
backend/.env
# Zips de credenciais (ex.: .env compactado pra transporte) — NUNCA versionar
*.zip
.trasncript.zip

# Gravações das reuniões (Etapa 5) — pesadas, não versionar
backend/recordings/

# Dependências
node_modules/

# Logs
*.log
npm-debug.log*
# Screenshots de referência (não versionar)
Captura de tela*.png
Screenshot*.png
